# geo-watch

Local-first CLI that monitors brand citations in AI model outputs.

## For AI agents

- `python scripts/watch.py` — CLI with init/run/report/export commands
- SQLite database at data/watch.db
- Sibling repos: geo-prompts (prompt sets), mcp-geo (MCP wrapper)
